import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
      light: '#fff',
      dark: '#fff'
    },
    secondary: {
      main: '#ea80fc',
      light: '#ffb2ff',
      dark: '#b64fc8'
    },
    text: {
      primary: '#fff'
    }
  }
})

export default theme
